import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    marginTop: 20,
    padding: 20,
  },
  text: {
    fontSize: 40,
    fontWeight: '100',
    marginBottom: 20,
  },
});

export default styles;
