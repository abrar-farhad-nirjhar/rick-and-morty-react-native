import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 20,
  },
  text: {
    fontSize: 40,
    fontWeight: '100',

  },
  backButtonContainer: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },

  iconMargin: {
    marginRight: 10
  }
});

export default styles;
