import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  properties: {
    fontSize: 20,
    fontWeight: '100',
    marginBottom: 10,
  },
  scrollView: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  scroll: {
    paddingBottom: 350,
  },
});

export default styles;
