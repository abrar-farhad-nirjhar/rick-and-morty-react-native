import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  properties: {
    fontSize: 20,
    fontWeight: '100',
    marginBottom: 10,
  },
  scrollView: {
    marginBottom: 100,
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  scroll: {
    paddingBottom: 300,
  },
});

export default styles;
