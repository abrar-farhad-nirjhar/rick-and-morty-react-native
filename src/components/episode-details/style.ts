import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  properties: {
    fontSize: 20,
    fontWeight: '100',
    marginBottom: 10,
  },
  scroll: {
    paddingBottom: 450,
  },
});

export default styles;
