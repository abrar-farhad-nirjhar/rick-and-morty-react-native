import {StyleSheet} from 'react-native';
import variables from '../../utils/variables';
const styles = StyleSheet.create({
  container: {
    height: variables.scrollViewHeight,
  },
});
export default styles;
